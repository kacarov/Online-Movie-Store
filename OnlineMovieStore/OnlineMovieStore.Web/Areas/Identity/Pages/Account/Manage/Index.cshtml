@page
@using OnlineMovieStore.Web.Models

@model OnlineMovieStore.Web.Areas.Identity.Pages.Account.Manage.IndexModel

@Html.Partial("_StatusMessage", Model.StatusMessage)

<div class="breadcrumbs">
    <a href="/Home/Index">Home</a>
    <span>Help Center</span>
</div>
<div id="exTab2" class="container col-md-8">

    <ul class="nav nav-tabs">
        <li class="active">
            <a href="#ordersDetails" data-toggle="tab">Orders Details</a>
        </li>
        <li>
            <a href="#financeSettings" data-toggle="tab">Deposits</a>
        </li>
        <li>
            <a href="#accountSettings" data-toggle="tab">Account Settings</a>
        </li>
    </ul>

    <div class="tab-content ">
        <div class="tab-pane active" id="ordersDetails">
            @await Html.PartialAsync("Shered/_OrderDetails.cshtml")
        </div>
        <div class="tab-pane" id="financeSettings">
            @*@await Html.PartialAsync("Shered/_Deposit.cshtml")*@
        </div>
        <div class="tab-pane bank" id="accountSettings">
            @*<div class="col-md-5 bank">
                    <form id="profile-form bank" method="post">
                        <div asp-validation-summary="All" class="text-danger"></div>
                        <div class="form-group bank">
                            <label asp-for="Username"></label>
                            <input asp-for="Username" class="form-control bank" disabled />
                        </div>
                        <div class="form-group bank">
                            <label asp-for="Input.Email"></label>
                            @if (Model.IsEmailConfirmed)
                            {
                                <div class="input-group bank">
                                    <input asp-for="Input.Email" class="form-control bank" />
                                    <span class="input-group-addon bank" aria-hidden="true"><span class="glyphicon glyphicon-ok text-success"></span></span>
                                </div>
                            }
                            else
                            {
                                <input asp-for="Input.Email" class="form-control bank" />
                                <button id="email-verification" type="submit" asp-page-handler="SendVerificationEmail" class="btn btn-link">Send verification email</button>
                            }
                            <span asp-validation-for="Input.Email" class="text-danger bank"></span>
                        </div>
                        <div class="form-group bank">
                            <label asp-for="Input.PhoneNumber"></label>
                            <input asp-for="Input.PhoneNumber" class="form-control bank" />
                            <span asp-validation-for="Input.PhoneNumber" class="text-danger bank"></span>
                        </div>
                        <button type="submit" class="btn btn-default bank">Save</button>
                    </form>
                </div>*@
            /**/
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}